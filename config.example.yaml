# Example configuration for PKM Tool
# Copy this to ~/.config/pkm-tool/config.yaml or ~/.pkm-tool.yaml

# Output settings (Phase 2)
output_filename_template: "{date} ({day_abbr}).{format}"  # Default template
output_directory: "."  # Default to current directory

# Template variables:
#   {date}     - Full date in YYYY-MM-DD format (e.g., "2025-11-22")
#   {year}     - Four-digit year (e.g., "2025")
#   {month}    - Two-digit month (e.g., "11")
#   {day}      - Two-digit day (e.g., "22")
#   {day_abbr} - Abbreviated day name (e.g., "Mon", "Fri")
#   {format}   - Output format extension ("md" for markdown, "json" for json)

# Section configuration for customizing markdown output
# sections:
#   # Custom section titles (override defaults)
#   titles:
#     calendar_events: "📆 My Schedule"
#     github_activities: "💻 Development"
#     atlassian_items: "🏢 Atlassian"
#     things_tasks: "✅ Completed Tasks"
#     wakatime_activities: "⏱️ Coding Time"
#     google_docs: "📝 Documents"
#     whoop_data: "💪 Health"
#     errors: "⚠️ Errors"
#
#   # Custom section order (all sections must be listed)
#   order:
#     - calendar_events
#     - github_activities
#     - atlassian_items
#     - things_tasks
#     - wakatime_activities
#     - google_docs
#     - whoop_data
#     - errors

# Apple Calendar (macOS only)
apple_calendar:
  enabled: true
  config: {}
# GitHub integration
github:
  enabled: true
  exclude_weekends: false  # Set to true to skip GitHub on weekends
  config:
  # Tokens can be stored securely with `pkm auth login github`
  # Legacy fallback (not recommended):
  # token: ghp_your_token_here
  # username: your_github_username
# Atlassian (Jira/Confluence)
atlassian:
  enabled: false
  config:
    base_url: https://your-domain.atlassian.net
    username: your.email@example.com
    api_token: your_api_token_here
# Things (macOS only)
things:
  enabled: true
  config:
  # Optional: custom database path
  # database_path: /path/to/Things Database.thingsdatabase/main.sqlite
# Wakatime
wakatime:
  enabled: false
  exclude_weekends: true  # Example: skip coding activity tracking on weekends
  config:
    api_key: waka_your_api_key_here
# Google Docs
google_docs:
  enabled: false
  config:
    client_id: your_google_client_id.apps.googleusercontent.com
    client_secret: your_google_client_secret
    scopes:
      - https://www.googleapis.com/auth/drive.readonly
      # Optional legacy fallback (not recommended)
      # access_token: your_access_token_here
whoop:
  enabled: false
  config:
# Preferred: store tokens via `pkm auth login whoop`
# Legacy fallback (not recommended)
# access_token: your_whoop_access_token_here
# or set WHOOP_ACCESS_TOKEN environment variable
